<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="proj.spring.mes.dao.mapper.ItemMapperDAO">

  <!-- 목록 -->
  <select id="selectItemList"
          resultType="proj.spring.mes.dto.ItemDTO">
    SELECT
      item_id,
      item_price,
      item_div,
      item_name,
      item_unit
    FROM V3_item
    ORDER BY item_name ASC
  </select>

  <!-- 단건 -->
  <select id="selectItemOne"
          parameterType="string"
          resultType="proj.spring.mes.dto.ItemDTO">
    SELECT
      item_id,
      item_price,
      item_div,
      item_name,
      item_unit
    FROM V3_item
    WHERE item_id = #{item_id}
  </select>

  <!-- 등록 -->
  <insert id="insertItem" parameterType="proj.spring.mes.dto.ItemDTO">
    INSERT INTO V3_item (
      item_id, item_price, item_div, item_name, item_unit
    ) VALUES (
      #{item_id}, #{item_price}, #{item_div}, #{item_name}, #{item_unit}
    )
  </insert>

  <!-- 수정 -->
  <update id="updateItem" parameterType="proj.spring.mes.dto.ItemDTO">
    UPDATE V3_item
    SET
      item_price = #{item_price},
      item_div   = #{item_div},
      item_name  = #{item_name},
      item_unit  = #{item_unit}
    WHERE item_id = #{item_id}
  </update>

  <!-- 삭제 -->
  <delete id="deleteItem" parameterType="string">
    DELETE FROM V3_item
    WHERE item_id = #{item_id}
  </delete>

</mapper>
